define(['./lib/DragBus', './lib/Insertion', './lib/install/group', './lib/mixin/index'], function (
               DragBus,         Insertion,                 group,         mixin){
    return {
        DragBus : DragBus,
        Insertion : Insertion,
        group : group,
        mixin : mixin
    };
});
