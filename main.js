define(['./lib/DragBus', './lib/install/group', './lib/mixin/index'], function (
               DragBus,                 group,         mixin){
    return {
        DragBus : DragBus,
        group : group,
        mixin : mixin
    };
});
